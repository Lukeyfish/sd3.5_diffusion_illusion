# Example configuration file (jobs.txt)

# Format: [job_name] parameters
# Lines starting with # are comments
# Empty lines are ignored

# Testing different methods
[attentions_1] --method spatial_attention --steps 50
[attentions_1] --method normalize --steps 50
[attentions_1] --method transformer --steps 50